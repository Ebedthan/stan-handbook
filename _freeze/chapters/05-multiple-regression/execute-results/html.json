{
  "hash": "f7f9c8c1dad7dccd6db6ca4bdc99340f",
  "result": {
    "engine": "knitr",
    "markdown": "# Multiple Regression and Predictors\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n## Overview\n\nChapter 4 introduced the Stan workflow for a single predictor. Real biological data almost always involve several drivers acting simultaneously like temperature *and* soil moisture, treatment *and* block, diet *and* blood pressure. This chapter extends the simple regression model to $K$ predictors, introduces the **matrix formulation** that makes Stan code scale gracefully to any number of predictors, and addresses two practical issues that arise whenever predictors multiply: **scaling** (so that coefficients are comparable) and **multicollinearity** (correlated predictors that make individual coefficients hard to identify).\n\nThe workflow is identical to Chapter 4, the Stan file changes but the R-side steps do not.\n\n**Learning objectives**\n\nBy the end of this chapter you will be able to:\n\n- Write a Stan program for multiple regression using the matrix formulation\n  (`matrix[N, K] X`, `vector[K] beta`).\n- Centre and scale predictors in R before passing them to Stan, and\n  back-transform posterior summaries to the original scale.\n- Interpret standardised coefficients and compare effect sizes across\n  predictors measured in different units.\n- Diagnose multicollinearity from posterior pair plots and variance\n  inflation factors, and explain how it manifests in the posterior.\n- Use the `loo` package to compare a full model against a reduced model.\n\n\n## The model {#sec-model-ch05}\n\nWith $K$ predictors $x_{i1}, x_{i2}, \\ldots, x_{iK}$, the model is:\n\n$$\ny_i \\sim \\text{Normal}\\!\\left(\\alpha + \\sum_{k=1}^{K} \\beta_k\\, x_{ik},\\;\n\\sigma\\right)\n$$\n\nWritten compactly with matrix notation:\n\n$$\n\\boldsymbol{y} \\sim \\text{Normal}(\\alpha + \\mathbf{X}\\boldsymbol{\\beta},\\;\n\\sigma\\,\\mathbf{I})\n$$\n\nwhere $\\mathbf{X}$ is the $N \\times K$ predictor matrix (rows = observations, columns = predictors), $\\boldsymbol{\\beta}$ is a $K$-vector of slopes. $\\sigma\\,\\mathbf{I}$ is the $N \\times N$ covariance matrix of the residuals which is a scaled identity matrix that express the assumption that observations are **independent** (off-diagonal zeros) and share a **common variance** $\\sigma^2$ (identical diagonal entries). This homoscedastic independence assumption is the same one implicit in the $\\varepsilon_i \\sim \\text{Normal}(0, \\sigma)$ notation of the scalar form; writing it as $\\sigma\\,\\mathbf{I}$ simply makes it explicit. Hierarchical models (Chapter 8) and spatial models relax this assumption by replacing $\\sigma\\,\\mathbf{I}$ with a structured covariance matrix.\n\n| Symbol | Dimension | Meaning |\n|---|---|---|\n| $\\alpha$ | scalar | intercept at predictor means |\n| $\\boldsymbol{\\beta}$ | $K \\times 1$ | slopes, one per predictor |\n| $\\mathbf{X}$ | $N \\times K$ | centred and scaled predictor matrix |\n| $\\sigma$ | scalar $> 0$ | residual standard deviation |\n\n: Notation for multiple regression. {#tbl-notation-ch05}\n\n\n## Running example: drivers of litter decomposition\n\nWe extend the Chapter 4 example. Decomposition rate $k$ (log-transformed, yr⁻¹) across West African forest-savanna plots is now modelled as a function of three drivers:\n\n- $x_1$: **mean annual temperature** (°C, centred), a direct metabolic driver of microbial activity.\n- $x_2$: **soil moisture** (%, centred), water availability limits decomposer communities during the dry season.\n- $x_3$: **litter C:N ratio** (centred), substrate quality; high C:N slows decomposition by nitrogen-limiting microbes.\n\nThese three predictors are ecologically motivated, measurable in the field, and partly correlated with each other, making this a realistic test case for multiple regression.\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nset.seed(2026)\nN <- 60\n\n# Simulate correlated predictors\n# Correlation structure: temp–moisture r=0.40, temp–CN r=-0.20,\n# moisture–CN r=-0.15\nSigma_X <- matrix(c(\n   1.00,  0.40, -0.20,\n   0.40,  1.00, -0.15,\n  -0.20, -0.15,  1.00\n), nrow = 3)\n\n# Cholesky-based multivariate normal simulation\nL_X <- chol(Sigma_X)\nX_raw <- matrix(rnorm(N * 3), nrow = N) %*% L_X\n\n# Scale to realistic units before centring\ntemp <- X_raw[, 1] * 2.5          # SD ≈ 2.5°C\nmoisture <- X_raw[, 2] * 8 + 35   # mean 35%, SD ≈ 8%\ncn_ratio <- X_raw[, 3] * 5  + 45  # mean 45, SD ≈ 5\n\n# True parameters (on centred-scaled predictors)\ntrue_alpha <-  1.20\ntrue_beta1 <-  0.08    # temperature\ntrue_beta2 <-  0.04    # soil moisture\ntrue_beta3 <- -0.06    # C:N ratio (negative: high C:N slows decomposition)\ntrue_sigma <-  0.22\n\n# Centre and scale each predictor (store means/SDs for back-transformation)\ncentre_scale <- function(x) (x - mean(x)) / sd(x)\n\nx1 <- centre_scale(temp)\nx2 <- centre_scale(moisture)\nx3 <- centre_scale(cn_ratio)\n\nX  <- cbind(x1, x2, x3)   # N × K predictor matrix\n\n# Simulate outcome\ny <- rnorm(N,\n           mean = true_alpha + \n             true_beta1 * x1 +\n             true_beta2 * x2 +\n             true_beta3 * x3,\n           sd = true_sigma)\n\n# Data frame for plotting\ndecomp_multi <- data.frame(\n  log_k = y,\n  temp_cs = x1,\n  moist_cs = x2,\n  cn_cs = x3\n)\n\n# Pairwise plot using base pairs() is fine but ggplot pairs via GGally\n# is nicer, fall back to a manual faceted scatter if GGally absent\nif (requireNamespace(\"GGally\", quietly = TRUE)) {\n  GGally::ggpairs(\n    decomp_multi,\n    columnLabels = c(\"log k\", \"Temp (cs)\", \"Moisture (cs)\", \"C:N (cs)\"),\n    upper = list(continuous = GGally::wrap(\"cor\", size = 3.5)),\n    lower = list(continuous = GGally::wrap(\"points\",\n                                           alpha = 0.5, size = 1.2,\n                                           colour = \"#2e7d6a\")),\n    diag = list(continuous = GGally::wrap(\"densityDiag\",\n                                           colour = \"#2e7d6a\", fill = \"#aec6c8\",\n                                           alpha = 0.6))\n  )\n} else {\n  # Fallback: simple correlation matrix as text\n  cat(\"Install GGally for the pairs plot: install.packages('GGally')\\n\")\n  print(round(cor(decomp_multi), 2))\n}\n```\n\n::: {.cell-output-display}\n![Pairwise relationships among the three predictors and the outcome (log decomposition rate). Predictors are centred and scaled. Mild positive correlation between temperature and soil moisture (r ≈ 0.40) is ecologically plausible in this system.\n](05-multiple-regression_files/figure-html/simulate-data-ch05-1.png){width=95%}\n:::\n:::\n\n\n\n\n\n\n\n\n::: {.callout-tip}\n**Centre *and* scale, not just centre.**  \nIn Chapter 4 we only centred the predictor (subtract mean). With multiple predictors measured in different units, degrees Celsius, percent, unitless ratio, we also **scale** (divide by SD) so that all $\\beta_k$ are in the same units: \"change in $\\log k$ per one-SD increase in predictor $k$\". This makes coefficients directly comparable and makes the `normal(0, 5)` prior equally appropriate for all of them. Back-transformation to original units is straightforward and shown in @sec-backtransform.\n:::\n\n\n## The Stan file {#sec-stan-file-ch05}\n\n```stan\n// Bayesian multiple linear regression with K predictors.\n//          y_i ~ Normal(alpha + X[i,] * beta, sigma)\n\ndata {\n  int<lower=1> N;   // number of observations\n  int<lower=1> K;   // number of predictors\n  matrix[N, K] X;   // predictor matrix (N rows, K columns)\n  vector[N] y;  // outcome\n}\n\nparameters {\n  real alpha;          // intercept\n  vector[K] beta;      // slopes (one per predictor)\n  real<lower=0> sigma; // residual SD\n}\n\nmodel {\n  // Priors\n  alpha ~ normal(0, 10);\n  beta ~ normal(0, 5);     // applied independently to each beta[k]\n  sigma ~ exponential(1);\n\n  // Likelihood\n  y ~ normal(alpha + X * beta, sigma);\n}\n\ngenerated quantities {\n  vector[N] y_rep;\n  vector[N] log_lik;\n\n  for (i in 1:N) {\n    real mu_i = alpha + dot_product(X[i], beta);\n    y_rep[i] = normal_rng(mu_i, sigma);\n    log_lik[i] = normal_lpdf(y[i] | mu_i, sigma);\n  }\n}\n```\n\n### What changed from Chapter 4\n\nThree things changed; everything else is identical:\n\n**`data` block:** \n- Added `int<lower=1> K` (number of predictors) and replaced `vector[N] x` with `matrix[N, K] X`.\n\n- The matrix type in Stan stores data in column-major order; rows are observations, columns are predictors.\n\n**`parameters` block:** \n- `real beta` became `vector[K] beta`. \n\n- Stan treats this as $K$ independent scalar parameters sharing the same prior.\n\n**`model` block:** \n- the likelihood is now `y ~ normal(alpha + X * beta, sigma)`.\n\n- The expression `X * beta` is matrix–vector multiplication: Stan computes $\\mathbf{X}\\boldsymbol{\\beta}$ in one vectorised operation, more efficient and readable than a double loop.\n\n::: {.callout-note}\n**`vector[K] beta ~ normal(0, 5)` is a vectorised prior.**  \nThis single line places an independent `Normal(0, 5)` prior on *each* element of `beta`. It is equivalent to writing:\n\n```stan\nfor (k in 1:K)\n  beta[k] ~ normal(0, 5);\n```\n\nbut is faster and more concise. The prior does not model correlation among the $\\beta_k$, they are treated as independent a priori. Chapter 8 introduces hierarchical priors that *do* model such correlation.\n:::\n\n## Compiling and sampling {#sec-fit-ch05}\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmod_multi <- cmdstan_model(\"stan/multiple_regression.stan\")\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Data list: note K is passed explicitly\nstan_data_multi <- list(\n  N = N,\n  K = ncol(X),\n  X = X,\n  y = y\n)\n```\n:::\n\n::: {.cell}\n\n```{.r .cell-code}\nfit_path_multi <- \"multiple_regression_fit.rds\"\n\nif (!file.exists(fit_path_multi)) {\n  fit_multi <- mod_multi$sample(\n    data = stan_data_multi,\n    seed = 2026,\n    chains = 4,\n    parallel_chains = 4,\n    iter_warmup = 1000,\n    iter_sampling = 1000,\n    refresh = 0\n  )\n  fit_multi$save_object(fit_path_multi)\n} else {\n  fit_multi <- readRDS(fit_path_multi)\n}\n```\n:::\n\n\n\n\n\n\n\n\n\n\n## Convergence diagnostics\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit_multi$summary(variables = c(\"alpha\", \"beta[1]\", \"beta[2]\", \"beta[3]\", \"sigma\"))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n#> # A tibble: 5 × 10\n#>   variable    mean  median     sd    mad        q5     q95  rhat ess_bulk\n#>   <chr>      <dbl>   <dbl>  <dbl>  <dbl>     <dbl>   <dbl> <dbl>    <dbl>\n#> 1 alpha     1.21    1.21   0.0265 0.0260  1.17      1.26   1.00     4842.\n#> 2 beta[1]   0.0498  0.0503 0.0302 0.0298 -0.000102  0.0995 1.00     4854.\n#> 3 beta[2]   0.0665  0.0661 0.0307 0.0311  0.0160    0.115  1.00     4159.\n#> 4 beta[3]  -0.0565 -0.0560 0.0279 0.0274 -0.103    -0.0113 1.000    4782.\n#> 5 sigma     0.206   0.205  0.0197 0.0189  0.176     0.241  1.00     4108.\n#> # ℹ 1 more variable: ess_tail <dbl>\n```\n\n\n:::\n:::\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nmcmc_trace(\n  fit_multi$draws(c(\"alpha\", \"beta[1]\", \"beta[2]\", \"beta[3]\", \"sigma\"))\n) +\n  labs(title = \"Trace plots of the multiple regression\")\n```\n\n::: {.cell-output-display}\n![Trace plots for the intercept, three slope parameters, and the residual SD. All chains mix well with no trend, drift, or separation.\n](05-multiple-regression_files/figure-html/fig-trace-ch05-1.png){#fig-trace-ch05 width=95%}\n:::\n:::\n\n\n\n\n\n\n\n\nAll five parameters show excellent mixing across the 1 000 sampling iterations: the four chains are indistinguishable throughout, with no trend, drift, or separation: convergence is not in doubt. The intercept $\\alpha$ oscillates tightly between 1.15 and 1.30, reflecting precise estimation of the mean log decomposition rate at average predictor values. The three slope chains tell a clear story about relative identifiability: $\\beta_1$ (temperature) and $\\beta_3$ (C:N ratio) fluctuate over ranges of roughly 0.05-0.15 and -0.15-0.05 respectively, while $\\beta_2$ (soil moisture) is centred closer to zero with chains spanning approximately -0.05 to 0.10, consistent with it being the weakest of the three predictors. The $\\sigma$ chains are the most compact of all, oscillating between 0.15 and 0.28, indicating that residual variance is well-constrained by $N = 60$ observations even after accounting for three predictors simultaneously.\n\n\n## Interpreting the posterior {#sec-interpret-ch05}\n\n### Coefficient plot\n\nA coefficient plot (also called a forest plot) is the standard display for multiple regression posteriors. It shows the posterior distribution of each slope on a common axis, making effect sizes directly comparable when predictors are standardised.\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\ndraws_multi <- as_draws_df(\n  fit_multi$draws(c(\"beta[1]\", \"beta[2]\", \"beta[3]\"))\n)\n\n# Rename for plotting\nnames(draws_multi)[1:3] <- c(\"Temperature\", \"Soil moisture\", \"C:N ratio\")\n\ncoef_df <- draws_multi |>\n  select(Temperature, `Soil moisture`, `C:N ratio`) |>\n  pivot_longer(everything(), names_to = \"predictor\", values_to = \"value\") |>\n  group_by(predictor) |>\n  summarise(\n    median = median(value),\n    lo50  = quantile(value, 0.25),\n    hi50 = quantile(value, 0.75),\n    lo90 = quantile(value, 0.05),\n    hi90 = quantile(value, 0.95),\n    .groups = \"drop\"\n  ) |>\n  mutate(\n    predictor = factor(predictor,\n                       levels = c(\"Temperature\", \"Soil moisture\", \"C:N ratio\")),\n    true_val  = c(true_beta3, true_beta2, true_beta1)[\n      match(predictor, c(\"C:N ratio\", \"Soil moisture\", \"Temperature\"))\n    ]\n  )\n\nggplot(coef_df, aes(y = predictor)) +\n  geom_vline(xintercept = 0, linetype = \"dashed\",\n             colour = \"grey60\", linewidth = 0.6) +\n  geom_segment(aes(x = lo90, xend = hi90, yend = predictor),\n               colour = \"#2e7d6a\", linewidth = 0.8) +\n  geom_segment(aes(x = lo50, xend = hi50, yend = predictor),\n               colour = \"#2e7d6a\", linewidth = 2.5) +\n  geom_point(aes(x = median),\n             colour = \"#1a4a40\", size = 3) +\n  geom_point(aes(x = true_val),\n             colour = \"#c0392b\", shape = 4, size = 3, stroke = 1.2) +\n  labs(\n    x = \"Standardised coefficient (change in log k per 1-SD increase)\",\n    y = NULL,\n    title = \"Posterior slopes, multiple regression\",\n    subtitle = \"Median, 50% CI (thick), 90% CI (thin), ✕ true value\"\n  )\n```\n\n::: {.cell-output-display}\n![Posterior distributions for the three slope parameters (standardised predictors). Points are posterior medians; thick bars are 50% credible intervals; thin bars are 90% credible intervals. Red dashed lines mark true data-generating values. The vertical line at zero is a reference: intervals that exclude zero indicate clear directional effects.\n](05-multiple-regression_files/figure-html/fig-coef-plot-1.png){#fig-coef-plot width=95%}\n:::\n:::\n\n\n\n\n\n\n\n\nThe coefficient plot reveals a clear and coherent picture of the three drivers of litter decomposition. Temperature ($\\beta_1$) and soil moisture ($\\beta_2$) both have positive effects, with their entire 90% credible intervals lying to the right of zero: the data leave no ambiguity about the direction of these effects. The C:N ratio ($\\beta_3$) is the only predictor with a negative coefficient, its 90% interval entirely to the left of zero, confirming that high C:N substrates slow decomposition as expected from stoichiometric theory. In terms of magnitude, temperature and C:N ratio are roughly symmetric around zero with standardised coefficients near $\\pm 0.08$, while soil moisture has a somewhat smaller effect near $+0.05$, consistent with it being the weakest predictor in the LOO comparison below. All three posterior medians (filled circles) fall close to but not exactly on the true data-generating values (red crosses), small discrepancies that are entirely expected sampling variation at $N = 60$ and serve as a healthy reminder that posterior means are estimates, not the truth.\n\n### Back-transforming to original units {#sec-backtransform}\n\nStandardised coefficients are convenient for comparison but biologists or ecologists often want effects in original units. Back-transformation is simple: divide the standardised coefficient by the SD of the original predictor.\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# Store the SDs used for scaling\nsd_temp <- sd(temp)\nsd_moisture <- sd(moisture)\nsd_cn <- sd(cn_ratio)\n\ndraws_raw <- as_draws_df(\n  fit_multi$draws(c(\"beta[1]\", \"beta[2]\", \"beta[3]\"))\n)\n\n# Back-transform: divide by predictor SD\nbeta1_orig <- draws_raw[[\"beta[1]\"]] / sd_temp\nbeta2_orig <- draws_raw[[\"beta[2]\"]] / sd_moisture\nbeta3_orig <- draws_raw[[\"beta[3]\"]] / sd_cn\n\ncat(\"Temperature (per °C):\",\n    round(mean(beta1_orig), 4),\n    \"  90% CI [\",\n    round(quantile(beta1_orig, 0.05), 4), \",\",\n    round(quantile(beta1_orig, 0.95), 4), \"]\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n#> Temperature (per °C): 0.0211   90% CI [ 0 , 0.0422 ]\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"true\"}\ncat(\"Soil moisture (per %):\",\n    round(mean(beta2_orig), 4),\n    \"  90% CI [\",\n    round(quantile(beta2_orig, 0.05), 4), \",\",\n    round(quantile(beta2_orig, 0.95), 4), \"]\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n#> Soil moisture (per %): 0.0076   90% CI [ 0.0018 , 0.0131 ]\n```\n\n\n:::\n\n```{.r .cell-code  code-fold=\"true\"}\ncat(\"C:N ratio (per unit):\",\n    round(mean(beta3_orig), 4),\n    \"  90% CI [\",\n    round(quantile(beta3_orig, 0.05), 4), \",\",\n    round(quantile(beta3_orig, 0.95), 4), \"]\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n#> C:N ratio (per unit): -0.0122   90% CI [ -0.0222 , -0.0024 ]\n```\n\n\n:::\n:::\n\n\n\n\n\n\n\n\nThese original-scale coefficients answer the practical question: *\"By how much does log decomposition rate change per 1°C increase in temperature, holding soil moisture and C:N ratio constant?\"*\n\n## Multicollinearity {#sec-multicol}\n\n**Multicollinearity** occurs when two or more predictors are correlated. It does not bias estimates, the posterior means remain valid, but it *inflates posterior uncertainty* for the correlated coefficients and makes them difficult to interpret individually.\n\n### Diagnosing multicollinearity\n\nThe classical frequentist tool is the **variance inflation factor** (VIF). A VIF $> 5$ is a conventional warning threshold; VIF $> 10$ is severe.\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# VIF requires the car package; base R alternative shown as fallback\nif (requireNamespace(\"car\", quietly = TRUE)) {\n  lm_for_vif <- lm(y ~ x1 + x2 + x3)\n  vif_vals <- car::vif(lm_for_vif)\n  knitr::kable(\n    data.frame(Predictor = c(\"Temperature\", \"Soil moisture\", \"C:N ratio\"),\n               VIF = round(vif_vals, 2)),\n    caption = \"Variance inflation factors. Values well below 5 indicate\n               no problematic multicollinearity in this dataset.\"\n  )\n} else {\n  # Manual VIF via R² of each predictor on the others\n  vif_manual <- function(X) {\n    sapply(seq_len(ncol(X)), function(j) {\n      r2 <- summary(lm(X[, j] ~ X[, -j]))$r.squared\n      1 / (1 - r2)\n    })\n  }\n  vif_vals <- vif_manual(X)\n  knitr::kable(\n    data.frame(Predictor = c(\"Temperature\", \"Soil moisture\", \"C:N ratio\"),\n               VIF = round(vif_vals, 2)),\n    caption = \"Variance inflation factors (manual computation).\"\n  )\n}\n```\n\n::: {.cell-output-display}\n\n\nTable: Variance inflation factors. Values well below 5 indicate\n               no problematic multicollinearity in this dataset.\n\n|   |Predictor     |  VIF|\n|:--|:-------------|----:|\n|x1 |Temperature   | 1.39|\n|x2 |Soil moisture | 1.21|\n|x3 |C:N ratio     | 1.18|\n\n\n:::\n:::\n\n\n\n\n\n\n\n\n\nHere we can see that all the predictors have a VIF value less than 5, suggesting a low correlation between them.\n\n### What multicollinearity looks like in the posterior\n\nWhen two predictors are highly correlated, their joint posterior is elongated along a ridge meaning that the data cannot distinguish their individual contributions, only their sum. @fig-multicol-demo illustrates this with a deliberately inflated correlation.\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nset.seed(7)\n\nsim_corr_posterior <- function(rho, n_draws = 2000) {\n  # Simulate joint posterior under known multicollinearity\n  # by refitting with inflated predictor correlation\n  Sigma2 <- matrix(c(1, rho, rho, 1), 2, 2)\n  X2 <- cbind(\n    X[, 1],\n    rho * X[, 1] + sqrt(1 - rho^2) * rnorm(N)\n  )\n  # OLS covariance approximation for illustration\n  XtX_inv <- solve(t(X2) %*% X2)\n  sigma2   <- var(residuals(lm(y ~ X2)))\n  Sigma_post <- sigma2 * XtX_inv\n  mvtnorm::rmvnorm(n_draws,\n                   mean  = c(mean(draws_multi$Temperature),\n                             mean(draws_multi$`Soil moisture`)),\n                   sigma = Sigma_post)\n}\n\nif (requireNamespace(\"mvtnorm\", quietly = TRUE)) {\n  d_low  <- as.data.frame(sim_corr_posterior(0.40))\n  d_high <- as.data.frame(sim_corr_posterior(0.90))\n  names(d_low) <- names(d_high) <- c(\"beta1\", \"beta2\")\n  d_low$correlation  <- \"r = 0.40 (actual)\"\n  d_high$correlation <- \"r = 0.90 (inflated)\"\n  d_both <- rbind(d_low, d_high)\n\n  ggplot(d_both, aes(beta1, beta2)) +\n    geom_point(colour = \"#2e7d6a\", alpha = 0.2, size = 0.6) +\n    geom_density2d(colour = \"#1a4a40\", linewidth = 0.5, bins = 6) +\n    facet_wrap(~correlation) +\n    labs(\n      x = expression(beta[1]~\"(Temperature)\"),\n      y = expression(beta[2]~\"(Soil moisture)\"),\n      title = \"Joint posterior under low vs high multicollinearity\"\n    )\n} else {\n  # Fallback: text description\n  cat(\"Install mvtnorm for the multicollinearity demo:\",\n      \"install.packages('mvtnorm')\\n\")\n\n  draws_multi |>\n    select(Temperature, `Soil moisture`) |>\n    ggplot(aes(Temperature, `Soil moisture`)) +\n    geom_point(colour = \"#2e7d6a\", alpha = 0.3, size = 0.8) +\n    geom_density2d(colour = \"#1a4a40\", linewidth = 0.5, bins = 6) +\n    labs(title = \"Joint posterior: temperature × soil moisture (r = 0.40)\")\n}\n```\n\n::: {.cell-output-display}\n![Joint posterior of β₁ (temperature) and β₂ (soil moisture) under low correlation (r = 0.40, left) and high correlation (r = 0.90, right). With high multicollinearity the posterior smears along a ridge: the individual coefficients are uncertain but their sum is not. This is not a sampling failure, it is the correct posterior given correlated predictors.\n](05-multiple-regression_files/figure-html/fig-multicol-demo-1.png){#fig-multicol-demo width=95%}\n:::\n:::\n\n\n\n\n\n\n\n\n::: {.callout-warning}\n**Multicollinearity does not bias posteriors, but it limits interpretation.**  \nIf VIFs are high, the posterior correctly reflects that the data cannot separately identify the correlated coefficients. The remedy is not to remove one predictor blindly, but to ask: *does the scientific question require distinguishing their individual effects?* If only the combined effect matters (e.g. a prediction task), high collinearity is harmless. If individual coefficients are the target, consider collecting more data, using ridge-like priors (Chapter 8), or restructuring the predictors.\n:::\n\n## Model comparison: full vs reduced model {#sec-model-comparison-ch05}\n\nDoes soil moisture (the weakest predictor) improve the model enough to justify its inclusion? We fit a reduced model without $x_2$ and compare using LOO-CV.\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# Fit the reduced model (temperature + C:N only) using the same Stan file\n# with K = 2 and X = cbind(x1, x3)\nstan_data_reduced <- list(N = N, K = 2, X = cbind(x1, x3), y = y)\n\nfit_reduced <- mod_multi$sample(\n  data = stan_data_reduced,\n  seed = 2026,\n  chains = 4,\n  parallel_chains = 4,\n  iter_warmup = 1000,\n  iter_sampling = 1000,\n  refresh = 0\n)\nfit_reduced$save_object(\"reduced_regression_fit.rds\")\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# Extract log-likelihoods and compute LOO\nloo_full <- fit_multi$loo(variables = \"log_lik\")\nloo_reduced <- fit_reduced$loo(variables = \"log_lik\")\n\n# Compare\nloo_compare <- loo::loo_compare(loo_full, loo_reduced)\nprint(loo_compare)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n#>   model elpd_diff se_diff p_worse diag_diff diag_elpd\n#>  model1       0.0     0.0      NA                    \n#>  model2      -1.0     2.7    0.65   N < 100\n```\n\n\n:::\n:::\n\n::: {.cell tbl-cap='LOO-CV comparison. The model listed first has higher expected log predictive density (ELPD). A difference larger than twice its standard error is considered meaningful.'}\n::: {.cell-output-display}\n\n\n|Model                  |model  | elpd_diff| se_diff| p_worse|diag_diff |diag_elpd | elpd_loo| se_elpd_loo| p_loo| se_p_loo| looic| se_looic|\n|:----------------------|:------|---------:|-------:|-------:|:---------|:---------|--------:|-----------:|-----:|--------:|-----:|--------:|\n|Full (3 predictors)    |model1 |         0|     0.0|      NA|          |          |      6.8|         7.8|   5.8|      2.1| -13.6|     15.7|\n|Reduced (2 predictors) |model2 |        -1|     2.7|     0.6|N < 100   |          |      5.8|         6.3|   4.2|      1.4| -11.6|     12.7|\n\n\n:::\n:::\n\n\n\n\n\n\n\n\n::: {.callout-note}\n**Interpreting LOO comparisons.**  \n`loo_compare()` ranks models by expected log predictive density (ELPD). The best model is listed first with `elpd_diff = 0`; subsequent rows show the difference from the best. A difference of `elpd_diff / se_diff` $> 2$ is a conventional threshold for preferring the top model. We return to model comparison in full in Chapter 14.\n:::\n\n## Posterior predictive check\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\ny_rep_multi <- as_draws_matrix(fit_multi$draws(\"y_rep\"))\n\nppc_dens_overlay(y, y_rep_multi[1:50, ]) +\n  labs(\n    title = \"Posterior predictive check of the full model\",\n    subtitle = \"Dark: observed. Light: 50 posterior predictive datasets\"\n  )\n```\n\n::: {.cell-output-display}\n![Posterior predictive density overlay for the full three-predictor model. The observed distribution (dark) is well-enveloped by the 50 replicated datasets (light), indicating adequate model fit.\n](05-multiple-regression_files/figure-html/fig-ppc-ch05-1.png){#fig-ppc-ch05 width=95%}\n:::\n:::\n\n\n\n\n\n\n\n\n## Comparison with `lm()`\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nlm_multi <- lm(y ~ x1 + x2 + x3)\n\nstan_coefs <- fit_multi$summary(c(\"alpha\", \"beta[1]\", \"beta[2]\", \"beta[3]\", \"sigma\")) |>\n  select(variable, mean, sd, q5, q95)\n\nlm_coefs <- data.frame(\n  variable = c(\"alpha\", \"beta[1]\", \"beta[2]\", \"beta[3]\", \"sigma\"),\n  ols_est = c(coef(lm_multi), sigma(lm_multi)),\n  ols_lo = c(confint(lm_multi, level = 0.90)[, 1], NA),\n  ols_hi = c(confint(lm_multi, level = 0.90)[, 2], NA)\n)\n\nmerge(stan_coefs, lm_coefs, by = \"variable\") |>\n  knitr::kable(\n    digits = 3,\n    caption = \"Stan posterior vs OLS for the three-predictor model.\",\n    col.names = c(\"Parameter\", \"Bayes mean\", \"Bayes SD\",\n                  \"Bayes 5%\", \"Bayes 95%\",\n                  \"OLS estimate\", \"OLS 5%\", \"OLS 95%\")\n  )\n```\n\n::: {.cell-output-display}\n\n\nTable: Stan posterior vs OLS for the three-predictor model.\n\n|Parameter | Bayes mean| Bayes SD| Bayes 5%| Bayes 95%| OLS estimate| OLS 5%| OLS 95%|\n|:---------|----------:|--------:|--------:|---------:|------------:|------:|-------:|\n|alpha     |      1.213|    0.026|    1.168|     1.256|        1.230|  1.181|   1.278|\n|beta[1]   |      0.050|    0.030|    0.000|     0.100|        0.081|  0.024|   0.139|\n|beta[2]   |      0.066|    0.031|    0.016|     0.115|        0.078|  0.024|   0.132|\n|beta[3]   |     -0.057|    0.028|   -0.103|    -0.011|       -0.055| -0.108|  -0.001|\n|sigma     |      0.206|    0.020|    0.176|     0.241|        0.225|     NA|      NA|\n\n\n:::\n:::\n\n\n\n\n\n\n\n\nAs in Chapter 4, point estimates are nearly identical. The value of the Bayesian framework becomes apparent in the next chapters, where grouped data, partial pooling, and non-Gaussian likelihoods make `lm()` either suboptimal or inapplicable.\n\n## Summary\n\n- Multiple regression in Stan requires only three changes from the simple\n  regression model: add `int K` and `matrix[N, K] X` to the `data` block,\n  change `real beta` to `vector[K] beta`, and update the likelihood to\n  `y ~ normal(alpha + X * beta, sigma)`.\n- **Always centre and scale** predictors before passing to Stan when\n  predictors are on different scales. Standardised coefficients are\n  directly comparable; back-transformation to original units is trivial.\n- A single `beta ~ normal(0, 5)` prior applies independently to all\n  $K$ slopes. Adjust the SD if prior knowledge constrains effect sizes\n  more tightly.\n- **Multicollinearity** inflates posterior uncertainty for individual\n  coefficients without biasing them. Diagnose with VIFs; interpret with\n  the joint posterior pair plot.\n- LOO-CV (`fit$loo()`) provides a principled, single-fit comparison\n  between nested or non-nested models. Full coverage is in Chapter 14.\n- The above workflow, compile, prepare data list, sample with save/reload,\n  check convergence, interpret, PPC, is identical to Chapter 4.\n\n## Exercises\n\n1. **Add a fourth predictor.** Simulate a fourth predictor (e.g. clay\n   content, %) correlated with soil moisture ($r \\approx 0.60$). Add it\n   to the Stan model. Do the VIFs for moisture and clay exceed 5? How does\n   the posterior for `beta[2]` (moisture) change?\n\n2. **Prior sensitivity on slopes.** Refit the full model with\n   `beta ~ normal(0, 0.5)` (a tight prior). At $N = 60$, does this\n   visibly pull the posteriors toward zero? Repeat with $N = 15$.\n   What does this tell you about the prior–data balance?\n\n3. **Unstandardised comparison.** Refit the model passing raw\n   (uncentred, unscaled) predictors. Compare ESS values between the\n   standardised and unstandardised fits. What do you conclude about the\n   computational cost of skipping standardisation?\n\n4. **Posterior probability of ordering.** Using the posterior draws,\n   compute $P(|\\beta_1| > |\\beta_2| \\mid \\text{data})$ — the probability\n   that temperature has a larger absolute effect than soil moisture. This\n   is trivial with posterior draws and impossible with a single-number\n   $p$-value.\n\n\n\n\n\n\n\n\n   ::: {.cell}\n   \n   ```{.r .cell-code}\n   mean(abs(draws_multi$Temperature) > abs(draws_multi$`Soil moisture`))\n   ```\n   :::\n\n\n\n\n\n\n\n\n5. **LOO sensitivity.** Refit both the full and reduced models with\n   `iter_sampling = 2000`. Do the `elpd_diff` and `se_diff` values change\n   meaningfully? What does this tell you about the stability of LOO\n   estimates?\n\n\n## Further reading\n\n- @gelman2020regression [Ch. 12]: multiple predictors, scaling, and\n  the bias-variance tradeoff in a Bayesian context.\n- @mcelreath2020rethinking [Ch. 5]: an extended treatment of\n  multicollinearity using directed acyclic graphs (DAGs) to reason\n  about which predictors to include.\n- @gelman2013bda [Ch. 14.2–14.3]: the matrix form of Bayesian\n  regression and the conjugate normal-inverse-$\\chi^2$ analysis.\n- Stan User's Guide, \"Regression Models\":\n  <https://mc-stan.org/docs/stan-users-guide/regression.html>",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}